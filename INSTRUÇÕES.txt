INSTRUÇÕES


BANCO DE DADOS

create database teste;
use teste;
create table usuarios(nome varchar(50), email varchar(60) primary key, usuario varchar(20), senha varchar(64), tipo varchar(6), pontos int);

IMPORTAÇÕES

INSTALAÇÕES
pip install flask
pip install mysql.connector
pip install --upgrade mysql-connector-python